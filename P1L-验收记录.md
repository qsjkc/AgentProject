# 小猪桌宠 P1-K 收敛与 P1-L 候选发布验收

日期：2026-09-05。候选版本：`1.1.0-rc.1`，Windows x64。

本轮完成首次引导自动化收敛、PostgreSQL 发布检查、候选安装包构建与打包程序 smoke。2026-09-11 已完成远端推送、服务器部署及真实 HTTPS 业务验收；详见 `P1L-部署验收记录.md`。完整桌面场景采用本地 stub，数据库检查采用独立真实 PostgreSQL；两者不是一条真实服务端到端链路。

## 已交付

| 项目 | 结果与证据 |
|---|---|
| 桌面轻量测试 | `desktop/` 下 `npm.cmd test` 通过 |
| 自动化运行器自检 | `npm.cmd run test:e2e:runner` 3 项通过，覆盖 CDP 响应匹配、本地接口、TEMP 清理边界 |
| Electron 完整场景 | `npm.cmd run test:e2e` 连续两次 9/9；记录见下方 |
| 后端回归 | `backend/` 下 `venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests`，52 项通过；测试数据库为 SQLite |
| PostgreSQL 14 | 空库到 014、合成 010 数据升级到 014、降级再升级、备份恢复读写、ORM 字段、经验写入、双会话里程碑竞争、重复 ACK、周回顾与漏斗读取通过；见 `P1L-postgres-verification.json` |
| Windows 候选包 | `Detachym-1.1.0-rc.1-x64.exe`；校验值与代码版本见 `P1L-release-manifest.json` |
| 打包程序 smoke | 三次启动 `win-unpacked/Detachym.exe`，真实 `app.asar` 的三个 renderer 均加载，显式设置测试环境变量仍不暴露 E2E 控制；登录、快捷聊天、重启保留账户/小猪、退出登录后重启通过 |
| CI/CD 配置 | CI 加入 PostgreSQL、Windows 九场景及 packaged smoke；CD 安装包文件名跟随版本。远端 Actions 五项任务全部通过，运行记录见部署验收记录 |

本轮修复了提醒列表重复请求与旧权限句柄、主面板重载时过早接收导航、导航拒绝后提示占用展示状态、窗口销毁时读取失效对象，以及快捷聊天焦点/会话同步竞态。已有接口、默认宠物和提醒规则保持原有设计。

## 可复核证据

- 九场景第一次通过：`desktop/artifacts/e2e/2026-09-05T09-15-52-100Z--25716--6c0609/run.json`。
- 九场景第二次通过：`desktop/artifacts/e2e/2026-09-05T09-16-41-793Z--31876--d42533/run.json`。
- 打包程序：`desktop/artifacts/e2e/packaged-1788599924967/packaged-smoke.json`。
- 每轮目录保存窗口截图、CDP 日志和主进程日志；测试账户和服务均为本机合成数据。userData 已清理。
- PostgreSQL 官方镜像 `postgres:14-alpine`，本轮 digest 为 `sha256:727876d274666da0b92a445390ba093c84b8e9f8343e1c53cd4e9a7ab2d85310`。项目原镜像镜像站本次返回 401，因此使用官方镜像验证，未修改生产镜像配置。

九场景为：三窗口、首次互动/关系发现/提醒送达完成、权限过期续用、主面板重载恢复导航、延迟响应下 A/B 账号切换、窗口销毁重建、ACK false、ACK 五秒超时、关系广播后快捷聊天。

测试将系统空闲固定为零、主动使 capability 过期、推进 stub 提醒到期时间。故障场景有明确状态 fixture；自然首次引导场景不重置引导状态。打包 smoke 使用强制终止后再启动来验证持久化，不证明优雅退出、系统崩溃或睡眠恢复。

## 重复验证

在 `desktop/` 执行：

```powershell
npm.cmd ci
npm.cmd test
npm.cmd run test:e2e:runner
npm.cmd run build:renderer
npm.cmd run test:e2e
npm.cmd run build
npm.cmd run test:packaged
```

在 `backend/` 执行（Docker 已启动，Python 已安装项目 requirements）：

```powershell
.\venv\Scripts\python.exe scripts\verify_postgres_release.py --report ..\P1L-postgres-verification.json
```

PostgreSQL 脚本只创建本次专属容器，不挂载宿主数据；自动清理 exact container ID。备份、回退使用合成数据，未读取生产库。部署回退仍需旧应用与旧库备份配套，不能把删除新表的 downgrade 当作生产无损回退。

## 安装与真实服务验收清单

候选包沿用本地构建配置的默认服务地址 `https://detachym.top/api/v1`；2026-09-11 已确认在线后端部署版本与可用性，但尚未执行桌面端连接真实后端的完整场景。登录页可修改地址。安装包未签名，不能因为 electron-builder 输出 signing 步骤就视为签名完成。

下一轮应在干净 Windows 用户或独立测试机中使用这一个固定校验值的安装包，填写下面各项的实际时间、结果与日志路径：

| 顺序 | 操作 | 通过条件 | 当前状态 |
|---|---|---|---|
| 1 | NSIS 安装、启动、卸载后重装 | 目录/快捷方式正确，三个窗口正常，无历史测试数据污染 | 待验收 |
| 2 | 连接明确版本的真实后端并创建全新账号，选择小猪 | 登录成功，关系归属正确；记录满足资格且可见活跃的 T0 | 待验收 |
| 3 | 首次观察、摸摸/拖动、打开关系页 | 真实关系卡可见，引导不中断语音/拖拽，不自动创建提醒 | 待验收 |
| 4 | 创建一分钟提醒，自然等待并完成 | 后端保存一条提醒，只送达一次；完成后引导结束，重启不重复创建 | 待验收 |
| 5 | A/B 切换、断网重连、退出后重启 | 不出现旧账号关系/提醒/聊天，不借用新凭据提交旧请求 | 待真实服务验收 |
| 6 | 两个桌面同时领取同一里程碑 | 服务端仅一个有效领取，播放与 ACK 可恢复，无重复经验/回礼 | 待双桌面验收 |
| 7 | 隐藏、系统空闲、睡眠唤醒、跨日运行 | 活跃预算正确暂停/恢复，提醒/回礼不重复，长时间运行无异常增长 | 待验收 |
| 8 | 生产同版本快照演练升级与恢复 | 记录应用版本、Alembic 版本、备份校验和恢复后业务读写 | 已完成生产快照恢复到独立克隆库、升级与业务检查；生产完成 003→014 迁移，原表数据指纹一致 |

2026-09-11 已配置 `DESKTOP_RELEASE_VERSION=1.1.0-rc.1`、`DESKTOP_RELEASE_FILE=Detachym-1.1.0-rc.1-x64.exe` 并发布对应安装包；线上版本接口及下载验证结果见部署验收记录。

## 小范围试用与下一阶段

上述必需验收通过后，再用固定候选版本开展 5–10 人、七天试用。现阶段只准备观察口径，不自动邀请或代发消息。

- 首日记录：安装/登录阻塞、能否自行找到互动与关系、是否主动创建并完成提醒、是否感到打扰。
- 次日记录：是否主动回来、回来做什么、提醒与关系状态是否连续。
- 第七日记录：仍然使用的理由、放弃的理由、周回顾展示/打开/后续提醒的聚合漏斗。
- 使用匿名参与编号、版本、日期、操作、预期/实际、严重程度、证据路径；不把聊天正文和 token 放进反馈表。
- 先修阻塞与重复提醒，再按反馈选择一项改进。若发现率低，优化现有入口；若价值不清，打磨提醒/关系反馈；若打扰过多，先调节奏与静音体验。暂不凭空追加新玩法。

本轮保留既有 Vite CJS 与大 chunk 警告。后端 52 项通过但有依赖弃用及测试连接清理警告；这些不被计为真实服务稳定性证据。
