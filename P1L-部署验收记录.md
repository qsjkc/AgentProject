# P1-L 服务器部署验收

2026-09-11 20:01（北京时间），`1.1.0-rc.1` 已部署，真实 HTTPS 业务验收通过。

## 版本与发布

- 远端分支：`dev_0811`；部署源码：`91afaf2bf3c35a6ff8a4860697f0fa115261cc5c`。
- [该提交的 CI](https://github.com/qsjkc/AgentProject/actions/runs/33958435466) 五项任务全部通过，包含真实 PostgreSQL 检查、Windows E2E 和打包程序 smoke。
- 服务器采用源码归档部署，211 个源码文件哈希核对通过；运行版本以 `.deployed-release.json`、镜像及文件校验为据。服务器旧 Git 工作区不作为运行版本证据。
- 后端镜像：`sha256:9c713082ca1f5aed5c0d237a0ca14b1a536107df7111d3047e77e27cca8fd7d8`。
- Nginx 镜像：`sha256:1cc8488061a9b9e6b2102d9bd99b41f1603285aa711ae66a8e55bda4d13357da`。
- 本记录属于后续文档提交，不改变上述部署源码版本。

## 数据库与业务结果

切换前停止旧后端并重新备份生产库，将 Alembic 从 `003` 升至 `014`。原有表的完整行数据指纹在迁移前后保持一致。另已从当天生产快照恢复到独立克隆库，完成升级与业务检查；克隆库已清理。

通过真实公网 HTTPS 验证以下业务，详见 [机器可读业务验收结果](P1L-server-deployment-verification.json)：

| 验收项 | 结果 |
|---|---|
| 登录、偏好持久化、未登录拒绝与账号隔离 | 通过 |
| 经验写入幂等、双请求竞争领取、重复 ACK、跨账号拒绝 | 通过 |
| 提醒保存、触发、完成幂等及账号隔离 | 通过 |
| 日/周回顾、重复提醒读取及管理员权限边界 | 通过 |
| 数据库版本、后端 readiness、Agent 健康、Nginx 配置 | 通过 |

测试仅对两个合成账号写入业务数据，结束后账号及关联测试记录已清理。后端为 `healthy`，Agent 健康接口返回 `ok`，切换后的检查未发现后端异常日志。

## 公网安装包

- [下载安装包](https://detachym.top/download/Detachym-1.1.0-rc.1-x64.exe)
- [在线版本接口](https://detachym.top/api/v1/public/version/win-x64)：`1.1.0-rc.1`，`available=true`。
- 大小：`128674973` 字节。
- 公网完整下载 SHA-256（与本地发布包一致）：`80f542484348d9a2cf45d7cf6d82450a540f89b1cb9ca3266660df136596d571`。
- 公网首页及两项静态资源检查、完整下载校验均通过，详见 [公网验收结果](P1L-public-deployment-verification.json)。

## 回退与验收边界

服务器 `/root/PersonalSpace/agentproject-releases/91afaf2/rollback/` 保留切换前数据库、运行源码和配置；旧应用镜像保留 `rollback-91afaf2` 标签。回退必须配套恢复应用与数据库，不能直接把删表 downgrade 当成无损回退；数据库恢复前需要处理切换后新增业务数据。

9 月 5 日构建期间曾出现服务器高负载及 SSH/HTTPS 超时，本次继续时服务已恢复。尚未确认故障根因；后续宜在构建环境产出镜像，减少在小规格生产机上构建的负担。

本次通过的是服务器部署及 API 业务验收。桌面九场景与打包程序 smoke 使用本地 stub；未执行干净 Windows 的 NSIS 安装卸载、桌面连接真实后端的完整链路、真实语音/模型生成，以及睡眠唤醒和跨日长时间运行。安装包未签名。上述项目保留在 [桌面验收清单](P1L-验收记录.md)，不计为本次已通过。
